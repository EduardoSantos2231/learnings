# L5 — Perguntas

**Q1 —** Qual a capacidade de um pipe no Linux? O que acontece quando o pipe enche?

**Q2 —** Qual a diferença entre um pipe anônimo (`os.Pipe`) e um named pipe (FIFO)?

**Q3 —** Como você passa um file descriptor do processo pai para o filho?
