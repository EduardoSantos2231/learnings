# L6 — Perguntas

**Q1 —** O que `2>&1` realmente faz no nível de file descriptor?

**Q2 —** Explique por que `cmd > file 2>&1` funciona mas `cmd 2>&1 > file` não faz o que você espera.

**Q3 —** Como você redirecionaria stderr para um arquivo diferente do stdout?
