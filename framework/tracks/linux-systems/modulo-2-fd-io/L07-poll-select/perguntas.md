# L7 — Perguntas

**Q1 —** Como `select()` difere de spawnar uma goroutine por FD? Quando select é melhor?

**Q2 —** Qual a diferença entre select, poll e epoll?

**Q3 —** Qual o número máximo de FD que `select()` consegue tratar? Por que isso é uma limitação?
