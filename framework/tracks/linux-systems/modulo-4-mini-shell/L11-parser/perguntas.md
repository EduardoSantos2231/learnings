# L11 — Perguntas

**Q1 —** Qual a diferença entre aspas simples e aspas duplas no shell? Como seu parser trata cada uma?

**Q2 —** O que acontece se o input for `echo "hello` (aspas não fechadas)? Como seu parser lida com isso?

**Q3 —** Por que separar tokenização de parsing (AST) é uma boa ideia? O que acontece se você misturar as duas etapas?
