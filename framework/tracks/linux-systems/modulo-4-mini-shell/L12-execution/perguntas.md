# L12 — Perguntas

**Q1 —** Por que `cd` precisa ser um builtin e não pode ser um executável externo?

**Q2 —** Como `os/exec.LookPath` encontra o executável? O que acontece se `PATH` estiver vazio?

**Q3 —** Qual a diferença entre `execve` e `fork+exec`? Qual o Go usa com `os/exec`?
