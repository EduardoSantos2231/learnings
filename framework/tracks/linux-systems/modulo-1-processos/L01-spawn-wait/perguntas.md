# L1 — Perguntas

**Q1 —** O que acontece com o processo filho se o pai morrer antes de chamar `Wait()`?

**Q2 —** Como `os/exec.Cmd` difere de `syscall.ForkExec`?

**Q3 —** Qual a diferença entre exit code 0, exit code 1 e exit code -1 (morto por sinal)?
