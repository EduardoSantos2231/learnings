# L8 — Perguntas

**Q1 —** Qual a diferença entre `os.Lstat` e `os.Stat`? Quando isso importa?

**Q2 —** Como você detecta um loop de symlink ao percorrer uma árvore de diretórios?

**Q3 —** Que informações você consegue obter de `FileInfo.Sys()` no Linux?
