# Perguntas — 01 hello-world

1. Qual a diferença entre **imagem** e **container**?
2. O que `docker ps -a` mostra que `docker ps` não mostra?
3. `docker run` vs `docker start` — qual a diferença?
4. Dá pra entrar em um container depois que ele finaliza? Por quê?
5. Rodar `docker run hello-world` duas vezes cria dois containers ou reusa o mesmo?
