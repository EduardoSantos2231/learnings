# Perguntas — D7 (Healthcheck)

> Responda em `respostas.md`. Inclua o campo de confiança (1-5) em cada resposta.

## Q1

Qual a diferença entre o container estar "unhealthy" e estar "stopped"?

## Q2

`--restart=on-failure` vs `--restart=always`: em qual situação `on-failure` é
melhor que `always`? Dê um exemplo concreto.

## Q3

O HEALTHCHECK é executado dentro do container ou pelo daemon Docker?
Como você prova isso?
