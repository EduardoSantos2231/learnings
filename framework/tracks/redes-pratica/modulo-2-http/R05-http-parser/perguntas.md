# R05 — Perguntas

### Q1

What makes HTTP parsing tricky with raw bytes (vs using a library)?

### Q2

How does `Content-Length` differ from `Transfer-Encoding: chunked` in terms of parsing?

### Q3

What would happen if a client sends headers without a `Host` header in HTTP/1.1?
