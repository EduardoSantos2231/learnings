# Perguntas — 02 interactive-shell

1. O que as flags `-i` e `-t` significam separadamente? E se você usar só `-i` sem `-t`?
2. `docker run -it` vs `docker exec -it` — qual a diferença?
3. O que acontece com o PID 1 dentro do container quando você dá `exit` no `docker run` vs `docker exec`?
4. Por que o arquivo criado em `/tmp` sobrevive a um `docker start`?
5. `ps aux` mostra processos diferentes dentro de `docker run` vs `docker exec` — por quê?
