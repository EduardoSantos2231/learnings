# Perguntas — 03 my-first-dockerfile

1. `CMD` vs `ENTRYPOINT` — qual a diferença?
2. O que acontece se você colocar **dois** `CMD` no mesmo `Dockerfile`?
3. Para que serve `WORKDIR`? O que acontece sem ele?
4. Por que a ordem das instruções no `Dockerfile` importa para o cache de build?
5. O que a flag `--rm` faz e quando usar?
6. O que `docker history` mostra?
7. `COPY` vs `ADD` — qual a diferença?
